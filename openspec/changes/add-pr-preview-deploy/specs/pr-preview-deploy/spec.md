## ADDED Requirements
### Requirement: Pull request preview deployment
The system SHALL publish a preview deployment for each pull request so reviewers can test changes before merge.

#### Scenario: Preview deployment available
- **WHEN** a pull request is opened or updated
- **THEN** a preview deployment URL is published for the pull request
